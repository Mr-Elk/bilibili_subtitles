# 项目治理

本仓库的正式职责是：安全提取 B 站已经提供的字幕，并对本地字幕执行限量、可验证的读取。它不是课程完成系统、自动笔记系统或模型摘要服务。

## 当前状态

- `main` 是唯一稳定底座，负责字幕提取、`manifest.json`、Status、Inventory、Map、Search、Slice、安装和验证。
- `feature/course-learning-source-pack` 只为未来的来源重构包试点保留。它可以快进接收 `main` 的治理提交，但在 AI-G0 通过或所有者明确替代该决定前，不实现模型调用或真实课程试点。
- 当前没有远端发布、版本标签或自动部署。Git 提交只证明源码历史，不证明安装、真实联网提取、课程处理或学习完成。

## 受保护边界

Git 只保存源码、测试、协议、提示模板和治理文档。下列内容不得提交：

- `.venv/`、修复备份、下载或运行缓存；
- `output/`、`.course-learning-private/` 和真实 `part-*.md`；
- Cookie、访问令牌、API key、私钥、授权头或浏览器配置；
- 真实模型请求、原始模型输出、审核记录、来源包和学习笔记。

默认匿名访问。读取 Chrome Cookie 必须由调用者逐次显式传入 `-UseBrowserCookies`。大型合集必须显式选择分 P，或使用 `-AllParts` 确认全量处理。

## 变更门禁

每个准备提交的变更必须：

1. 只暂存明确文件，不把工作区中的其他材料带入提交；
2. 运行 `git diff --check`；
3. 运行 `./verify.ps1`，通过依赖、测试、编译、PowerShell 语法和跟踪文件边界检查；
4. 涉及安装器时，再核对安装副本与源码哈希；
5. 涉及真实 B 站行为时，使用临时输出目录做最小验证，并单独报告联网、登录态和平台响应边界；
6. 不把离线测试描述成真实联网验证，也不把提取成功描述成字幕正确或学习完成。

## 运行可观察性与中断

- 提取进度只写入 `stderr`，最终摘要写入 `stdout`；本地 JSON 读取的标准输出必须始终保持可解析。
- 进度事件只能包含阶段、耗时、分 P 和固定状态，不得包含字幕正文、Cookie、授权头或完整响应。
- PowerShell 启动器必须实时转发提取错误流，不能等 Python 退出后才显示进度。
- 只有在公开字幕回退已启用时，才可收敛 yt-dlp 的“字幕需登录”预期警告；其他警告仍须上报，真实失败由带阶段上下文的错误统一报告。
- `Ctrl+C` 或其他基础异常必须清理 staging；原子发布中断时必须恢复旧输出，并保留下一次运行的恢复能力。
- 网络超时、HTTP 错误、接口业务错误、响应格式错误和安全边界错误必须可区分并带阶段上下文。没有真实失败证据前不增加自动重试；权限、格式与安全边界错误永不重试。

需要形成可交付快照时，在提交后运行：

```powershell
.\verify.ps1 -RequireClean
```

`-StaticOnly` 只用于临时 Git 测试夹具验证文件、凭据和工作树规则。它会跳过 Python、依赖、测试和编译，并只输出 `STATIC-ONLY PASS`；不能作为提交、安装或交付证据。

## 依赖治理

- `requirements.txt` 表达直接依赖意图；`requirements-lock.txt` 固定 Windows 运行环境的完整解析结果。
- `setup.ps1` 只从锁文件安装。不得把自动升级到最新版本作为安装步骤。
- 更新依赖时必须同时更新两个文件，从新建虚拟环境安装，运行 `pip check` 和完整验证，并记录平台与 Python 版本。
- 锁文件固定版本，但不等于供应链签名或离线安装保证；下载源和包哈希若成为发布要求，需要另立发布设计。

## 安装发布

- 安装文件必须先在目标目录暂存并校验；向后兼容的 schema v1 配置先发布，PowerShell 实现随后发布，CMD 入口最后发布，避免新实现读取旧配置。
- 覆盖已有安装时，每个受管文件都必须保留事务备份；发布或用户环境更新失败时恢复旧文件和旧环境。只有回滚本身失败时才保留恢复文件并要求人工处理。
- `-NoUserEnvironment` 只允许写入显式安装目录和配置，不得修改用户 PATH、用户环境变量或发送环境变更广播；它用于便携安装和隔离集成验证。
- 安装验证必须比较源码、暂存副本和最终副本的 SHA-256，并证明失败注入后旧入口可用且没有事务文件残留。

## 数据与协议

- `manifest.json` schema v1 是后续本地工具的稳定目录接口；Markdown 服务于人工阅读。
- 工具只拥有顶层 `manifest.json`、`index.md` 和严格命名的 `part-NNN.md`。其他文件和目录属于用户；读取必须忽略，全量刷新必须原样保留。
- manifest 必须继续拒绝缺失正文和孤立的严格 `part-NNN.md`，但 `part-NNN-learning-note-draft.md` 等非规范文件不是字幕，也不是 manifest 孤立项。
- `Status` 必须按 schema v1 严格核对清单与字幕文件。只有清单确实不存在时才允许旧 Markdown 迁移；清单存在但损坏、版本不支持或文件不一致时必须失败关闭。
- 原字幕文件保持只读语义。任何校正、推断或摘要必须写入独立成果，并保留时间戳、来源和不确定性。
- `no_subtitles` 是显式覆盖状态，不得伪造空字幕或把缺失字幕当作提取成功。
- `READER_JSON_V1.md` 的协议变化需要新版本或向后兼容测试。

## 提炼功能解冻条件

只有以下任一事实成立，才允许在保留分支上开始 `course_learning` 实现：

1. 权威规划中的 AI-G0 已通过，并有明确替换任务和容量；或
2. 所有者明确废止原等待决定，并重新批准范围、验收、隐私和停止条件。

解冻不自动授权云端提交。真实模型调用仍需独立确认供应商、模型、预算、训练使用、保留删除条款和运行时批准。
